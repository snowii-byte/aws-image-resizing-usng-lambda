# aws-image-resizing-usng-lambda
Serverless image resizing project using AWS Lambda and S3. When an image is uploaded to the source bucket, it triggers a Lambda function that resizes the image using Pillow and stores it in a destination bucket. Fast, scalable, and automated image processing system.
